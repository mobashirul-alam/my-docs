const authConfig = {
    providers: [
        {
            domain: "https://emerging-mule-56.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
